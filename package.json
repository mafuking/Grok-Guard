{
  "name": "grok-egress-guard",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "description": "Local quality and egress probe for official-login Cursor, Grok Build, and Grok CLI",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test src/lib/*.test.js",
    "install:hooks": "node scripts/install-hooks.mjs"
  }
}
