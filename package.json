{
  "name": "grok-egress-guard",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "description": "Local quality and egress probe for official-login Cursor, Grok Build, and Grok CLI",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test src/lib/score.test.js",
    "ext:compile": "npm --prefix cursor-extension run compile",
    "install:cursor": "node scripts/install-cursor.mjs"
  }
}
